import { describe, it } from 'vitest';
describe('AviationModule schema', () => {
  it('placeholder — fails until schema is generated', () => {
    // intentionally empty — run: npm run prisma:generate
  });
});
